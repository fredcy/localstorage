## Example localstorage application

Run `modd` from command line to build and serve this test application via devd.

### Testing localstorage disabled

In the Firefox "about:config" page, set `dom.storage.enabled` to False, then try the application. You should see "NoStorage" errors being reported in the "Errors" section of the page.
